version: 2

models:
  - name: silver_bookings
    columns:
      - name: booking_id
        tests:
          - not_null
          - unique

      - name: listing_id
        tests:
          - not_null

  - name: silver_hosts
    columns:
      - name: host_id
        tests:
          - not_null
          - unique

  - name: silver_listings
    columns:
      - name: listing_id
        tests:
          - not_null
          - unique